{
  "name": "gigflow-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "GigFlow Backend API",
  "main": "server.js",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "build": "cd ../frontend && npm install && npm run build",
    "build:frontend": "cd ../frontend && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.3",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "socket.io": "^4.6.1"
  }
}

